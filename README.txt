This is a basic setup for register/login authentication.
- Uses node.js, epxress, ejs, bcrypt, mongoose, session.
- Password hashing for security.
- Uses session and stores and holds the current user logged in. (Note: it uses user id instead of any personal info of the user.)
